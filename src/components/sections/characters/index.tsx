import { styled } from '@stitches/react'

const Box = styled('div', {})

const CharactersSection = () => {
  return (
    <Box as="section">
      <Box as="h2">CHARACTERS</Box>
    </Box>
  )
}

export default CharactersSection
