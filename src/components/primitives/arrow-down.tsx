import IconArrow from 'logos/arrow-hero.svg'
import { styled } from '../../../stitches.config'

const ArrowDown = styled(IconArrow, {
  $$size: 'max(20px, 1.389vw)',
  width: '$$size',
  height: '$$size'
})

export default ArrowDown
