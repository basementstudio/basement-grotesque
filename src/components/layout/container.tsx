import { styled } from '../../../stitches.config'

const Container = styled('div', {
  padding: '0 28px',
  mx: 'auto',
  position: 'relative',

  '@bp2': {
    padding: '0 40px'
  }
})

export default Container
